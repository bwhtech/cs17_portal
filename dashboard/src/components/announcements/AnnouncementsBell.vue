<template>
	<!-- Placeholder. W1 owns `components/announcements/*` and replaces this file
	     with the real bell: a `Popover` on desktop, a `BottomSheet` below `sm`,
	     an unread count, and per-announcement dismissal. It reads its own cohort
	     from `useSession`, so the props stay empty.

	     Showing nothing until then is deliberate — a bell that opens on an empty
	     list reads as "no announcements", which is a lie. -->
	<span hidden />
</template>

<script setup lang="ts"></script>
